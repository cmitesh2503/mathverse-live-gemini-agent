name: app
description: MathVerse AI Tutor

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  http: ^1.2.0
  camera: ^0.10.5
  image: ^4.1.7
  speech_to_text: ^6.6.2
  flutter_tts: ^3.8.5
  image_picker: ^1.0.7
    
dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true