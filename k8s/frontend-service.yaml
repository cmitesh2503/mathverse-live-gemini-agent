apiVersion: v1
kind: Service
metadata:
  name: mathverse-frontend-service
spec:
  type: LoadBalancer
  selector:
    app: mathverse-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
